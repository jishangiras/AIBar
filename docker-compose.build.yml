services:
  builder:
    build:
      context: .
      dockerfile: Dockerfile.build
    volumes:
      # built bundles land here on the host
      - ./dist:/app/dist
    environment:
      - RUST_LOG=info
